# IBC 프로토콜 보안 모델 분석

IBC(Inter-Blockchain Communication) 프로토콜은 서로 다른 블록체인 간 안전한 통신을 가능하게 하는 표준화된 프로토콜이다. 본 문서에서는 서로 다른 체인 간 신뢰할 수 있는 통신을 보장하기 위해 IBC에서 사용하는 보안 모델과 CometBFT+PoS 환경에서 발생 가능한 다양한 상황에 대해 기술한다.

<br>

## IBC 보안 모델

IBC 프로토콜은 서로 다른 두 체인간 토큰과 데이터를 전송할 수 있도록 상호운용성을 제공해주는 인터체인 프로토콜이다. IBC 보안 모델은 이러한 상호운용성 환경이 안전하게 유지되고 각 체인이 서로를 신뢰할 수 있도록 하기 위해 사용되는데, 대표적으로 상대 체인의 풀 노드가 아닌 최소한의 요소를 가지고 상대 체인을 검증하기 위한 클라이언트가 있다.

<br>

## Client

클라이언트(Light Client)는 통신하고 있는 상대 체인의 상태를 지속적으로 확인하며 체인 내부에 존재한다.

일반 노드와는 달리, 클라이언트는 모든 트랜잭션과 블록 데이터를 저장하지 않고, 상대 체인의 상태를 검증하기 위한 최소한의 요소만을 갖는다. 그리고 릴레이어로부터 상대 체인에 대한 정보를 지속적으로 업데이트받아서 최신 상태를 유지한다. 

클라이언트는 각각 고유의 식별자(Client ID)와 ClientState, ConsensusState를 갖는다. 
- ClientState: 클라이언트의 상태를 나타낸다. 상대 체인에 대한 식별자(ChainID)부터 상대 체인의 최근 블록 높이(LatestHeight)와 같은 정보를 포함한다.
- ConsensusState: 상대 체인의 타임스탬프, 다음 검증자 집합 해시, 상대 체인의 루트 해시를 포함한다. 

```go
interface ConsensusState {
  timestamp: uint64
  nextValidatorsHash: []byte
  commitmentRoot: []byte
}
```

ConsensusState는 릴레이어로부터 상대체인의 상태를 주기적으로 받아 업데이트하는 곳으로, 특히 상대 체인의 어플리케이션 상태에 대한 해시값을 표현하는 commitmentRoot는 IBC에서 데이터 패킷과 함께 상대 체인으로부터 전달되는 머클증명(Merkle Proof)을 검증하는데 사용되는 중요한 항목이다.

IBC에서 각 체인은 직접 연결되지 않고 클라이언트를 기반으로 연결된다. 그렇기 때문에 각 체인에 대한 검증은 모두 클라이언트를 통해 진행되며, 실제로 체인간 데이터 전송을 위해 생성하는 연결(connection), 채널(channel) 모두 클라이언트를 기반으로 생성된다. 이러한 이유로 Client ID는 IP 주소에, 각 체인의 ID는 DNS에 비유되기도 한다.

<br>

## Relayer

오프체인 기반 프로세스로, 클라이언트의 상태를 업데이트하고 체인 간 패킷을 전달한다. IBC에서는 각 체인이 직접 상대 체인과 데이터(트랜잭션)를 주고 받지 않고 릴레이어(Relayer)가 데이터 전송 요청을 확인하고 이에 해당하는 데이터를 패킷 형태로 대신 전달해준다. 

릴레이어는 각 체인의 풀노드에서 제공하는 Websocket 또는 RPC 엔드포인트로부터 받은 이벤트 정보와 머클 증명을 이용하여 클라이언트의 정보를 업데이트한다. 머클 증명은 이벤트 정보가 생성될 때 온체인에 저장되어 있다가, 릴레이어가 해당 이벤트를 패킷형태로 구성하여 상대 체인에 있는 클라이언트로 보낼때 함께 전송한다. 클라이언트는 머클 증명과 commitmentRoot를 비교한 검증이 완료되면 실제 패킷으로 전달된 트랜잭션을 내부에서 수행한다.

중요한 것은 릴레이어는 트랜잭션에 대한 검증은 전혀 하지 않으며, 오로지 체인 간 패킷으로 구성된 데이터와 증명에 필요한 내용들을 전달하기만 하고 모든 검증은 패킷을 받은 체인의 클라이언트에서 진행된다. 이는 검증 과정을 데이터를 중계하는 곳, 브릿지를 신뢰하는 것이 아닌 연결된 각 상대 체인을 신뢰함으로써 더욱 안전하고 효과적인 네트워크 연결이 가능하다는 장점이 있다.


<br>

## Merkle Proof

IBC는 블록에 존재하는 트랜잭션의 상태를 IAVL 트리 구조로 구성한다. 

IAVL 트리 구조는 내부 노드의 높이차를 1로 보장하기 때문에 최대 O(logn)의 삽입, 탐색, 삭제 연산속도를 지니는 장점이 있다. 트리를 구성하는 노드는 아래와 같다.

- 리프(leaf) 노드 : 머클 트리의 가장 하위에 위치하는 노드 (또는 검증하고자 하는 트랜잭션)
- 형제(aunt) 노드 : 리프 노드 중 검증하고자 하는 트랜잭션에 대한 노드를 제외한 동일 레벨에 있는 노드
- 루트(root) 노드 : 머클 트리에 존재하는 모든 트랜잭션이 포함된 상태를 나타내는 최상위 노드 (머클 루트)
![IAVL 트리 예시](https://miro.medium.com/v2/resize:fit:4800/format:webp/0*dNKVM51P2334adsu.png)


IBC에서는 체인 간 전송된 패킷 데이터가 실제로 발신 체인의 상태에 올바르게 기록된 내용인지 확인하기 위해 머클 증명을 사용한다. 패킷 데이터는 체인의 어플리케이션 모듈에서 생성되며, 이 과정에서 발신 체인은 해당 패킷이 블록에 기록되었음을 증명하기 위한 머클 증명(Merkle Proof)을 생성한다. 

머클 증명은 IVAL 트리에서 리프 노드에 위치하는 모든 트랜잭션 값들(검증하고자 하는 트랜잭션과 동일한 레벨에 위치한 형제 노드에 있는 트랜잭션 목록)을 포함하고 있는데, 상위 노드로 점점 계산되다가 마지막에 클라이언트의 ConsensusState commitmentRoot 값과 동일하다면 해당 트랜잭션이 블록에 정확히 존재한다는 것을 증명하게 된다.


아래 함수는 IBC 프로토콜에서 특정 데이터의 유효성을 검증하기 위해 머클 증명을 사용한다.

```go
func (proof MerkleProof) VerifyMembership(
  specs []*ics23.ProofSpec, // 머클 트리 구조 및 노드에 대한 스펙으로, 리프 노드의 계산 방식 및 해시 결합 방식에 대한 규격
  root exported.Root,       // 머클 트리의 루트 해시값
  path exported.Path,       // 머클 트리 내에서 데이터가 위치하고 있는 경로
  value []byte              // 검증하고자 하는 실제 데이터 값
) error {

  // 전달받은 파라미터 유효성 검사
  if err := proof.validateVerificationArgs(specs, root); err != nil {...}

	// MerklePath로 타입 변경 및 경로, spec의 길이 일치 여부 확인
	mpath, ok := path.(v2.MerklePath)	

  // 검증
	return verifyChainedMembershipProof(root.GetHash(), specs, proof.Proofs, mpath, value, 0)
}
```

타입 변경 및 유효성 검사가 완료되면 verifyChainedMembershipProof 함수를 통해 머클 검증을 수행한다. 이 함수가 리프 노드와 형제 노드로부터 값을 계산해서 최종적으로 머클 루트 값과 비교한다.

<br>

## Validator Set

새로운 블록이 생성되거나 체인의 상태가 변경되면, 릴레이어는 이를 감지하고 필요한 클라이언트에 업데이트 요청(MsgUpdateClient)을 전송한다. 이때 릴레이어는 검증에 필요한 헤더 값을 메시지에 담아 클라이언트에 전송한다. 

아래는 IBC 프로토콜에서 사용되는 헤더의 인터페이스로, 헤더는 아래 인터페이스를 참조한다.

```go
interface Header extends TendermintSignedHeader {
  identifier: string  // 클라이언트 식별자
  validatorSet: List<Pair<Address, uint64>> // 해당 블록의 검증자 집합(검증자 주소와 검증자 보유 지분으로 구성)
  trustedHeight: Height // ConsensusState에 등록된, 검증을 완료한 가장 최신 블록의 높이
  trustedValidatorSet: List<Pair<Address, uint64>>  // ConsensusState에 등록된 블록의 검증자 집합 
}
```

클라이언트의 ConsensusState 내부의 nextValidatorsHash 값이 trustedValidatorSet의 해시값과 동일하다면, 검증자 집합에 대한 유효성이 통과되고 validatorSet의 해시값이 새로운 nextValidatorsHash 값으로 설정되어 다음번 validator set 검증에 사용된다.

### CometBFT

CometBFT는 Tendermint 기반의 블록체인에서 사용하는 BFT 계열 합의알고리즘으로, 스테이킹 된 자산에 따라 검증자의 투표권이 결정되며 검증에 참여한 validator 의 지분(voting power)의 합이 전체 지분의 2/3+ 의 경우 합의에 성공하여 블록이 커밋된다. 지분이 많은 검증자가 블록을 제안할 확률이 높은 PoS 방식과 다르게, CometBFT는 Round-Robin 방식으로 무작위 선정된 검증자가 블록을 제안하는 역할을 맡는다.

#### IBC 보안 모델과 CometBFT 관계

클라이언트는 신뢰 가능한 최신 블록의 검증자 집합에 대한 해시를 기준으로 새로운 블록에 대한 검증자 집합을 비교하여 체인의 상태를 검증하기 때문에, 체인 내에 2/3+ 이상의 지분을 가진 정직한 검증자 집합은 필수적으로 존재해야 한다. 만약 악의적인 의도를 갖는 검증자가 가진 지분의 합이 전체 지분의 1/3 이상을 차지하는 경우, 검증자로 인한 올바른 블록 생성이 어려워지고 클라이언트는 상대 체인에 대한 신뢰를 계속 유지할 수 없게 된다. 특히 과반수 이상의 지분 합을 지닌 악의적인 검증자 집합이 공격을 시도할 경우, 잘못된 블록을 신뢰함으로 인해 데이터 무결성이 손상되는 문제가 발생할 수 있다. 

지분의 크기에 따른 블록 검증 권한이 부여되는 PoS 구조를 갖기 때문에, 지분이 많은 검증자 혹은 검증자 집단에서 올바르지 않은 블록을 무분별하게 증명하게 되는 Noting at Stake와 이로 인하여 잘못된 거래 내역이 계속해서 유지되는 Long Range Attack과 같은 문제점들은 발생 가능한 위험 요소가 될 수 있어 조심해야 한다.

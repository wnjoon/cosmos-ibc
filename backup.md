Validator Set 검증 과정은 IBC에서 매우 중요한 부분입니다. 특히, 다음 블록이 올바르게 생성되었는지를 검증할 때 Validator Set의 검증이 필요합니다. 이 과정은 클라이언트가 상대 체인의 상태를 신뢰할 수 있도록 하는 중요한 역할을 합니다. 아래에 Validator Set 검증 과정을 단계별로 설명합니다.

1. Validator Set의 정의

	•	Validator Set은 블록체인의 특정 시점에서 블록을 생성하고 검증하는 역할을 담당하는 검증자(Validator)들의 집합입니다.
	•	각 Validator는 특정 양의 스테이크(stake)를 걸고, 블록의 유효성을 검증합니다.
	•	Validator Set은 특정 블록의 검증자들로 구성된 집합체이며, 이들의 집합 해시(validator set hash)는 블록의 상태와 연계되어 클라이언트의 consensusState에 저장됩니다.

2. 클라이언트 생성 시 Validator Set 설정

	•	클라이언트가 생성될 때, 초기 Validator Set이 trustedValidatorSet으로 설정됩니다. 이 Validator Set은 클라이언트가 신뢰하는 이전 블록의 검증자 집합을 의미합니다.
	•	**trustedValidatorSet**은 이후 새로운 블록이 검증될 때 참조하는 기준이 됩니다.

3. UpdateClient 시 Validator Set 업데이트

	•	UpdateClient 메시지가 체인 B의 클라이언트에 도달하면, 클라이언트는 최신 상태로 업데이트됩니다.
	•	**신뢰할 수 있는 높이(trustedHeight)**에 있는 Validator Set(trustedValidatorSet)은 다음 블록의 검증에 사용됩니다.
	•	클라이언트는 Header 내에 포함된 새로운 Validator Set(validatorSet)과 현재 신뢰하고 있는 trustedValidatorSet을 비교합니다.

4. Validator Set 검증 과정

	•	과정:
	•	체인 B의 클라이언트는 체인 A의 새로운 블록을 업데이트하기 위해 전달받은 **Header**의 validatorSet을 검증합니다.
	•	클라이언트는 새로운 블록이 생성되기 전에 해당 블록의 검증자(Validator) 집합이 올바른지 확인하기 위해 trustedValidatorSet과 비교합니다.
	•	구체적으로, 클라이언트는 2/3 이상의 검증자가 새로운 블록에 서명했는지 검증합니다. 이 검증은 validatorSet의 해시와 trustedValidatorSet의 해시를 비교하는 방식으로 이루어집니다.

5. Merkle Proof와 Validator Set 검증

	•	Validator Set 검증은 주로 블록의 유효성을 확인하기 위한 것이며, 이 과정에서 Merkle Proof는 사용되지 않습니다.
	•	Merkle Proof는 주로 트랜잭션이나 상태가 블록체인의 Merkle Tree에 올바르게 포함되었는지를 검증하는 데 사용됩니다.
	•	반면, Validator Set 검증은 블록 자체의 유효성을 확인하기 위한 과정입니다.

6. 검증 성공 시

	•	클라이언트 업데이트: 검증이 성공하면, 클라이언트의 상태가 최신 블록의 Validator Set으로 업데이트됩니다.
	•	다음 Validator Set 준비: 새로운 Validator Set이 trustedValidatorSet으로 설정되며, 이는 이후 블록의 검증에 사용됩니다.

7. 검증 실패 시

	•	검증 실패: 만약 검증 과정에서 문제가 발생하면, 클라이언트 업데이트는 실패하며, 체인 B는 해당 블록을 신뢰할 수 없게 됩니다.
	•	에러 처리: 릴레이어는 오류를 보고하고, 필요한 경우 다시 시도하거나 다른 검증자를 통해 재검증을 요청할 수 있습니다.

요약

Validator Set 검증 과정은 블록체인이 올바르게 동작하는지 확인하는 핵심 과정입니다. IBC에서는 클라이언트가 신뢰할 수 있는 Validator Set을 기준으로 새로운 블록의 유효성을 검증하며, 이 과정에서 2/3 이상의 서명이 필요합니다. 이 검증이 완료되면 클라이언트는 최신 상태로 업데이트되며, 다음 블록을 위한 새로운 Validator Set이 설정됩니다.

----


Validator Set 검증과 Merkle Proof 검증의 관계

IBC에서 Validator Set 검증과 Merkle Proof 검증은 서로 다른 두 가지 중요한 검증 메커니즘입니다. 이 두 검증은 체인의 무결성과 신뢰성을 유지하기 위해 협력하지만, 그 역할과 검증 순서는 다릅니다.

1. Validator Set을 통한 검증

Validator Set 검증은 새로운 블록의 유효성을 확인하기 위한 과정입니다. 클라이언트는 최신 상태를 유지하기 위해 새로운 블록을 받아올 때, 이 블록이 올바르게 생성되었는지를 확인하기 위해 Validator Set을 검증합니다.

	•	Validator Set 검증 과정:
	•	클라이언트는 업데이트된 블록 헤더를 받아들여야 하며, 이 블록이 올바르게 생성되었는지 확인하기 위해 기존의 신뢰할 수 있는 trustedValidatorSet과 새로운 validatorSet을 비교합니다.
	•	이 비교 과정에서 2/3 이상의 서명이 올바르게 되어 있는지 확인합니다.
	•	결과:
	•	검증 성공 시: 클라이언트의 상태가 최신 상태로 업데이트됩니다.
	•	검증 실패 시: 블록의 유효성이 확인되지 않아 클라이언트의 상태가 업데이트되지 않습니다.

2. Merkle Proof를 통한 패킷 검증

Merkle Proof 검증은 패킷(예: 트랜잭션 데이터)이 실제로 체인의 상태에 기록되었는지를 검증하는 과정입니다. 이는 Validator Set 검증과는 다른 메커니즘으로 동작합니다.

	•	Merkle Proof 검증 과정:
	•	패킷이 체인 B로 전송되면, 이 패킷이 체인 A에서 실제로 기록되었음을 증명하기 위해 Merkle Proof가 사용됩니다.
	•	체인 B의 클라이언트는 업데이트된 commitmentRoot를 사용해 Merkle Proof를 검증합니다.
	•	결과:
	•	검증 성공 시: 패킷이 올바르게 기록되었음을 확인하고, 체인 B는 이 데이터를 신뢰합니다.
	•	검증 실패 시: 패킷이 조작되었거나 유효하지 않음을 의미하며, 체인 B는 이 데이터를 거부합니다.

두 검증의 관계와 순서

	1.	순서적 관계:
	•	Validator Set 검증이 먼저 이루어져야 합니다. 클라이언트가 최신 상태(즉, 최신 블록 헤더와 Merkle Root)로 업데이트된 후에야, 해당 블록에서 발생한 패킷에 대한 Merkle Proof 검증을 진행할 수 있습니다.
	•	Validator Set 검증은 블록의 무결성을 확인하기 위한 과정이고, Merkle Proof 검증은 특정 트랜잭션이나 상태가 블록체인에 기록되었음을 확인하기 위한 과정입니다. 따라서 Validator Set 검증이 성공적으로 이루어져야만, 해당 블록에 기록된 패킷에 대한 Merkle Proof 검증이 의미를 가집니다.
	2.	독립적인 검증 메커니즘:
	•	이 두 검증 메커니즘은 독립적으로 동작합니다. 하지만, Merkle Proof 검증은 클라이언트가 최신 상태로 업데이트된 후에만 의미가 있습니다. 따라서 Validator Set 검증이 완료된 후에 Merkle Proof 검증이 이루어지는 것이 일반적입니다.

3. Validator Set의 변화와 검증

Validator Set은 블록체인의 운영 환경에 따라 변경될 수 있습니다. 검증자 집합은 고정된 것이 아니라, 일정 주기나 블록 높이에 따라 변경될 수 있습니다.

	•	변화의 이유:
	•	검증자 추가/제거: 검증자는 스테이킹 상태나 네트워크 정책에 따라 변경될 수 있습니다.
	•	서명 비율: 특정 블록을 생성하기 위해, 현재 Validator Set의 2/3 이상의 서명이 필요합니다. 이는 블록의 무결성과 합의 상태를 유지하기 위한 중요한 기준입니다.
	•	검증의 필요성:
	•	매번 블록이 생성될 때마다, 이 블록이 올바르게 검증되었는지 확인하기 위해 Validator Set을 검증합니다.
	•	검증자가 변경되면, 새로운 Validator Set이 클라이언트에 반영됩니다.

결론

	•	Validator Set 검증은 블록의 무결성을 확인하기 위한 선행 검증입니다. 이 검증이 성공해야 클라이언트는 최신 상태로 업데이트되며, 이후에 발생한 패킷에 대한 Merkle Proof 검증이 가능해집니다.
	•	두 검증 메커니즘은 각각 독립적으로 동작하지만, Merkle Proof 검증은 Validator Set 검증 후에 의미를 가집니다.
	•	Validator Set은 고정되지 않고, 블록 생성에 따라 변할 수 있으며, 클라이언트는 이 변화를 반영해 최신 상태를 유지합니다.


----

이전 검증자 집합(Validator Set) 정보로 현재 검증자 집합 검증

Validator Set 검증은 새로운 블록이 올바르게 생성되었는지를 확인하는 과정에서 중요한 역할을 합니다. 이 검증은 클라이언트가 가지고 있는 이전 검증자 집합의 정보(예: 해시)를 기반으로 새로운 블록의 검증자 집합이 유효한지를 판단하는 방식으로 이루어집니다.

1. 검증자 집합 해시 비교의 기본 개념

	1.	이전 Validator Set 정보:
	•	클라이언트는 신뢰할 수 있는 이전 블록의 검증자 집합에 대한 정보를 가지고 있습니다. 이 정보는 보통 해시 형태로 저장되며, 이는 클라이언트의 trustedValidatorSet으로 불립니다.
	•	이 해시는 이전 블록의 검증자들이 서명한 블록의 해시로, 클라이언트가 신뢰하는 기준이 됩니다.
	2.	현재 Validator Set 정보:
	•	새로 생성된 블록에는 새로운 검증자 집합이 포함되어 있으며, 이 집합은 블록 헤더의 일부로 포함됩니다.
	•	클라이언트는 이 블록 헤더에 포함된 validatorSet의 해시를 통해 새로운 블록이 올바르게 검증되었는지를 판단합니다.

2. 검증 과정

	1.	체인 A에서 새 블록 생성:
	•	체인 A의 검증자들이 새 블록을 생성합니다. 이 블록에는 블록의 상태와 함께, 해당 블록을 검증한 검증자들의 집합에 대한 해시값이 포함됩니다.
	2.	Validator Set 변경 사항 반영:
	•	새로운 블록이 생성될 때, 이전 검증자 집합(이전 블록에서 유효했던 trustedValidatorSet)의 해시를 사용하여, 이 검증자들이 새로 제안된 블록에 서명했는지 확인합니다.
	•	각 검증자는 블록에 서명하면서 자신의 서명을 포함시킵니다.
	3.	2/3 이상 서명 검증:
	•	클라이언트는 새로운 블록의 헤더에 포함된 validatorSet의 해시를 확인하고, 이 해시가 이전 검증자 집합의 2/3 이상 서명한 것과 일치하는지 비교합니다.
	•	이 비교는 BFT(비잔틴 장애 허용) 합의 알고리즘에 따라 이루어지며, 합의가 제대로 이루어졌는지를 확인합니다.
	4.	검증 성공 시:
	•	만약 이전 검증자 집합의 2/3 이상이 새로운 블록에 서명했고, 새로운 블록의 validatorSet 해시가 유효하다면, 클라이언트는 이 블록을 신뢰하고, 상태를 업데이트합니다.
	5.	검증 실패 시:
	•	만약 검증이 실패하면, 클라이언트는 이 블록을 신뢰하지 않으며, 상태 업데이트가 이루어지지 않습니다.

3. 결론

이전 검증자 집합의 해시는 새로운 블록이 올바르게 생성되었는지를 확인하는 데 사용됩니다. 클라이언트는 이전 블록의 검증자 집합을 기준으로 새로운 블록의 검증자 집합이 유효한지를 판단하며, 이를 통해 블록체인의 무결성과 신뢰성을 유지합니다.

------



1. 클라이언트의 commitmentRoot 업데이트 시점
체인 B의 클라이언트는 체인 A의 상태를 추적하기 위해 생성된 클라이언트입니다. 이 클라이언트가 commitmentRoot를 업데이트하는 시점은 **상대 체인(A 체인)**에서 새로운 블록이 생성되거나, 상태가 변경될 때 릴레이어를 통해 이루어집니다. 업데이트 과정은 다음과 같이 진행됩니다:

릴레이어가 상태 동기화 요청:

릴레이어는 체인 A의 최신 블록이 생성되었거나, 중요한 상태 변경이 발생하면, 이 정보를 포함한 MsgUpdateClient 메시지를 체인 B로 전송합니다. 이 메시지에는 체인 A의 최신 상태와 관련된 데이터(예: 새로운 Merkle Root, 블록 헤더 정보)가 포함됩니다.
클라이언트의 상태 업데이트:

체인 B의 클라이언트는 이 메시지를 수신한 후, UpdateClient 함수가 호출되어 클라이언트의 상태를 최신 상태로 업데이트합니다. 이 과정에서 새로운 commitmentRoot가 클라이언트의 상태로 기록됩니다.
이후, 체인 B는 이 업데이트된 commitmentRoot를 기반으로 체인 A에서 전송된 데이터의 무결성을 검증할 수 있게 됩니다.
2. Merkle Proof의 리프 노드와 Aunt 노드
리프 노드(Leaf Node):
리프 노드는 Merkle Tree의 가장 하위에 위치한 노드로, 트랜잭션의 해시값이 직접 포함된 노드입니다.
Merkle Proof에서 검증하려는 특정 트랜잭션의 해시값이 리프 노드로 사용되며, 이 값을 기반으로 상위 노드들의 해시값을 계산해 나갑니다.
Aunt 노드(Aunt Nodes):
Aunt 노드는 Merkle Tree의 검증 경로에서 리프 노드의 형제 노드들을 말합니다. 이 노드들은 Merkle Proof에 포함되어, 리프 노드에서부터 루트 노드까지 해시 연산을 통해 최종적으로 Merkle Root에 도달할 수 있게 해줍니다.
Aunt 노드는 동일한 레벨에 위치한 형제 노드(리프 노드의 경우 다른 트랜잭션 해시값이 될 수 있음)들이며, 이 노드들은 검증 과정에서 중요한 역할을 합니다.
예시 설명
Merkle Tree 구조:

리프 노드: 트랜잭션 A의 해시값 (hash(Transaction A)).
리프 노드의 Aunt 노드: 동일한 레벨에 위치한 다른 트랜잭션 B의 해시값 (hash(Transaction B)).
상위 노드: hash(hash(Transaction A) + hash(Transaction B))와 같은 방식으로 계산됩니다.
검증 과정:

체인 B는 트랜잭션 A의 해시값과 그에 따른 Aunt 노드들의 해시값을 사용하여 상위 노드로 올라가면서 해시 연산을 수행하고, 최종적으로 Merkle Root에 도달합니다.
이 Merkle Root가 클라이언트에 저장된 commitmentRoot와 일치하면, 트랜잭션 A가 실제로 체인 A에 기록된 데이터임을 확인할 수 있습니다.
요약
클라이언트의 commitmentRoot 업데이트: 릴레이어가 최신 상태를 동기화하기 위해 MsgUpdateClient 메시지를 전송할 때 이루어집니다. 이는 체인 B의 클라이언트가 최신 Merkle Root를 기반으로 데이터를 검증할 수 있게 하기 위함입니다.
Merkle Proof의 리프 노드는 검증하려는 트랜잭션의 해시값이며, Aunt 노드는 동일한 레벨에 위치한 형제 노드들의 해시값입니다. 이들 노드를 통해 최종적으로 Merkle Root를 계산하고, 클라이언트에 저장된 값과 비교하여 데이터의 무결성을 검증합니다.

-----


2. 추가 예외 상황
2.1. 시빌 공격(Sybil Attack)
예외: 시빌 공격은 공격자가 다수의 가짜 신원을 만들어 네트워크를 장악하려는 시도입니다. PoS 시스템에서는 공격자가 다수의 가짜 검증자 노드를 생성해 네트워크에 영향을 미치려 할 수 있습니다.
영향: 검증자 집합의 다수가 악의적인 노드로 구성될 경우, 클라이언트가 잘못된 상태를 신뢰하게 될 위험이 있습니다.
2.2. 롱 레인지 공격(Long-Range Attack)
예외: 과거의 검증자 집합이 변경되었을 때, 공격자가 이 과거 상태로 돌아가 새롭게 검증된 블록을 생성하려는 시도입니다. PoS 환경에서는 이전에 스테이킹된 자산이 해제된 후, 공격자가 그 자산을 사용해 공격할 수 있습니다.
영향: 클라이언트가 과거의 검증자 집합을 신뢰하고 이를 기반으로 상태를 업데이트하면, 공격자가 이를 악용해 체인을 장악할 수 있습니다.
2.3. 악성 클라이언트 업데이트(Malicious Client Update)
예외: 릴레이어가 악성 클라이언트 업데이트를 시도하여, 클라이언트가 잘못된 상태를 업데이트하게 만들 수 있습니다.
영향: 클라이언트가 잘못된 상태를 신뢰하면, 이후의 모든 데이터 전송이 잘못된 상태를 기반으로 수행되어 데이터 무결성이 손상될 수 있습니다.
3. 추가 공격 시나리오
3.1. 퍼블릭 키 재사용 공격
설명: 공격자가 동일한 퍼블릭 키를 여러 검증자 집합에서 재사용하여 네트워크에 영향을 미치려는 시도입니다. 이는 PoS 시스템에서 키 관리가 부실할 때 발생할 수 있습니다.
대응: 검증자의 퍼블릭 키가 고유하며, 재사용되지 않도록 하는 강력한 키 관리 시스템이 필요합니다.
3.2. 슬래싱 조건 우회
설명: 공격자가 슬래싱(잘못된 행위에 대한 페널티)을 피하기 위해 검증자가 악의적인 행동을 해도 슬래싱을 피할 수 있는 방법을 찾는 경우입니다.
대응: 슬래싱 조건을 강화하고, 검증자가 악의적인 행동을 할 경우 이를 감지할 수 있는 메커니즘이 필요합니다.
3.3. 스테이트 드리프트(State Drift)
설명: 네트워크 지연이나 악성 행동으로 인해 클라이언트의 상태가 실제 체인의 상태와 일치하지 않게 되는 경우입니다.
대응: 클라이언트는 정기적으로 체인의 상태와 동기화하고, 네트워크 지연을 최소화할 수 있는 메커니즘이 필요합니다.
